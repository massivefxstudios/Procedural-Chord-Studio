{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "🎵 Chord Studio (Python Server + Chrome)",
            "type": "chrome",
            "request": "launch",
            "url": "http://localhost:8000",
            "webRoot": "${workspaceFolder}/ArrangerApp",
            "preLaunchTask": "Start Python Server"
        },
        {
            "name": "🌐 Chrome (Server zaten çalışıyor)",
            "type": "chrome",
            "request": "launch",
            "url": "http://localhost:8000",
            "webRoot": "${workspaceFolder}/ArrangerApp"
        }
    ]
}